class Solution{
    static int findSingle(int n, int arr[]){
       int result=0;
       for(int i=0;i<arr.length;i++){
           int num=arr[i];
           result^=num;
       }
       return result;
    }
}
