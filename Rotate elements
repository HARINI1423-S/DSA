class Compute {
    
    public void rotate(int arr[], int n)
    {
        int a[] = new int[n];
        a[0] = arr[n-1];
        for(int i = 1; i <= n-1; i++){
            a[i] = arr[i-1];
        }
        for(int i = 0; i < n; i++){
            arr[i] = a[i];
        }
    }
}
